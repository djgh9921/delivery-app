export interface Restaurant {
    id: string
    name: string
    description: string
    image_url: string
    address: string
    phone: string
    rating: number
    delivery_time: string
    min_order: number
    delivery_cost: number
    is_active: boolean
    created_at: string
}